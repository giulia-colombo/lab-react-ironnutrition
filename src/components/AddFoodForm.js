import { Input } from 'antd';

function AddFoodForm(props) {
    <Input name={name} value={} type="text" onChange={} />
    <Input name={image} value={} type="text" onChange={} />
    <Input name={calories} value={} type="text" onChange={} />
    <Input name={servings} value={} type="text" onChange={} />
}

